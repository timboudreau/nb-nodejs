{
    "name": "${projectName}",
    "description": "${description_escaped}",
    "version": "0.0.1",
    "main": "./${projectName}.js",
    "author": {
        "name": "${author}",
        "email": "${email}"
    },
    "keywords": [],
    "licenses": "${license}",
    "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1"
    },    
    "repository": {
        "type": "git",
        "url": "git://github.com/${login}/${projectName}.git"
    },
    "bugs": {
        "url": "http://github.com/${login}/${projectName}/issues"
    }
}
